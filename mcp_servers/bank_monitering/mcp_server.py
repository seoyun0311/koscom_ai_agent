# [bank_monitoring/mcp_server.py]

from mcp.server.fastmcp import FastMCP

# app_mcp.tools 패키지 전체 register 함수 가져오기
from app_mcp.tools import register as register_all_tools

# MCP 인스턴스 생성
mcp = FastMCP("bank-monitoring")


# ─────────────────────────────────────────
# Tool 등록 (통합)
# ─────────────────────────────────────────

register_all_tools(mcp)   # ← 모든 Tool 자동 등록 (🔥 핵심 변경점)


# ─────────────────────────────────────────
# ping 테스트용 (헬스체크)
# ─────────────────────────────────────────

async def ping() -> str:
    """서버 연결 확인용 간단 ping 함수"""
    return "pong"

# ping 도 MCP Tool로 등록
mcp.add_tool(
    ping,
    name="ping",
    description="서버 연결 확인용 ping 함수",
)


# ─────────────────────────────────────────
# 엔트리포인트
# ─────────────────────────────────────────

if __name__ == "__main__":
    print("✅ bank_monitoring MCP Server starting...")
    print(" - tools loaded by app_mcp.tools.register()")
    mcp.run(transport="stdio")
